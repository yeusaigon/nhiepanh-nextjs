# TODO

- [ ] **API Upload**: `/api/upload/route.ts` không hoạt động với `output: 'export'` (static export không hỗ trợ API route). Cần tách thành Cloudflare Worker riêng để xử lý upload R2. Xem thêm: https://developers.cloudflare.com/workers/
